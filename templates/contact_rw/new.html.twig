{% extends '@Sepulture/base.html.twig' %}


{% block body %}

    <div class="card"><h3 class="card-header">Nouveau contact</h3>
        <div class="card-body">

            {{ include('@Sepulture/contact_rw/_form.html.twig') }}

            <a href="{{ path('contact_rw_index') }}">
                <span class="fas fa-list"></span> Annuler et retour à la liste
            </a>
        </div>
    </div>
{% endblock %}

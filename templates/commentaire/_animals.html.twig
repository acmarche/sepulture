{% for animal in animals %}
    <button onclick="void(0);" type="button">
        <img src="{{ animal }}" rel="nofollow" alt="animal" class="animal" style="width: 120px;" data-url="{{ animal }}">
    </button>
{% endfor %}

{% extends '@Sepulture/base.html.twig' %}

{% block body %}

    <div class="card">
        <h3 class="card-header">Nouveau défunt pour la sépulture <strong>{{ sepulture.parcelle }}</strong>
        </h3>
        <div class="card-body">

            {% include '@Sepulture/defunt/form.html.twig' %}

        </div>
        <div class="card-footer">
            <a href="{{ path('sepulture_show', { 'slug': entity.sepulture.slug }) }}">
                <span class="ti ti-list"></span> Annuler et retour à la sépulture
            </a>
        </div>
    </div>
{% endblock %}

{% if is_granted('ROLE_SEPULTURE_EDITEUR') %}
    <div class="hidden-print">
        <div class="btn-toolbar" role="toolbar">
            <div class="btn-group">
                <a href="{{ path('sepulture_show', { 'slug': entity.sepulture.slug }) }}" role="button"
                   class="btn btn-primary"><i class="fas fa-list"></i> Retour à la sépulture</a>
            </div>
            <div class="btn-group">
                <a href="{{ path('defunt_edit', { 'id': entity.id }) }}" role="button"
                   class="btn btn-primary"><i class="fas fa-pencil-alt"></i> Editer</a>
            </div>
        </div>
    </div>
{% endif %}

{% extends '@Sepulture/base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}

    <div class="page-header">
        <h3>{{ user }}</h3>
    </div>

    {{ include('@Sepulture/user/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('user_index') }}">Retour à la liste</a>

    {{ include('@Sepulture/user/_delete_form.html.twig') }}
{% endblock %}
